[package]
name = "ti-toolbox-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xmltree = "0.11"
zip = { version = "2.2", default-features = false, features = ["deflate"] }
